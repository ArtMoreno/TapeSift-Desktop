"""TapeSift V2 presentation layer.

The V2 UI deliberately reuses the original widgets and all existing business
logic.  Keeping it in a separate package makes it possible to compare the two
interfaces against the same real projects without risking the daily editor.
"""
